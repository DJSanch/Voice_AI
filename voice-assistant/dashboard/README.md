# Astra Command Center

Start Astra in background mode:

```bash
cd voice-assistant
python3 main.py --background
```

Open `http://localhost:8080`. Astra starts the local-only Command Center server automatically; its buttons submit commands directly to the running assistant.
